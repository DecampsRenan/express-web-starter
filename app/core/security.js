let securityConfig = require(`${__dirname}/../config/security`);

// Here we define a kind of proxy middleware (like Symphony's FOSUserBundle)
let proxy = (req, res, next) => {
  
}

module.exports = proxy;